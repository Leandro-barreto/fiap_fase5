"""Route definitions for the hiring prediction API.

The ``routes`` subpackage contains FastAPI routers for each endpoint exposed
by the application.  Currently, only the prediction endpoint is
implemented in ``predict.py``.
"""
