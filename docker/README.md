# 🐳 docker/ - Containerização

Orquestração com `docker-compose.yml` para subir API, Prometheus e Grafana.

## Subir tudo
```bash
docker-compose up --build
```
- Prometheus: http://localhost:9090
- Grafana: http://localhost:3000 (admin/admin)
