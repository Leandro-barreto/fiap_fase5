# 🤖 src/ - Treino e Features

Scripts para geração de features (ex.: `sim_tfidf`, `overlap_kw`, `same_state`, `same_region`), treino (LightGBM) e avaliação (AUC, matriz de confusão).

## Saídas esperadas
- `models/lgbm_model.joblib`
- métricas e gráficos SHAP
